---
phase: 05-informational-pages-consultation-form
plan: 02
type: execute
wave: 2
depends_on:
  - "05-01"
files_modified:
  - src/components/contact/ConsultationForm/ConsultationForm.tsx
  - src/components/contact/ConsultationForm/ConsultationForm.module.css
  - src/components/contact/ContactHub/ContactHub.tsx
  - src/components/contact/ContactHub/ContactHub.module.css
  - src/app/contact/page.tsx
autonomous: true
requirements:
  - INFO-03
user_setup: []
estimate:
  tokens: 45000
  raw_tokens: 22000
  tasks: 2
  confidence: high
must_haves:
  truths:
    - "D-04: /contact page features a bespoke consultation inquiry form tailored for luxury residential builds."
    - "D-05: Form captures project location, architectural status, project scope, budget range, and contact information."
    - "D-06: Interactive selection pills provide clear tactile feedback with instant validation."
    - "D-07: Studio contact hub displays direct phone, email, and West Footscray studio location."
    - "'npx tsc --noEmit' and 'npm run build' compile cleanly."
  artifacts:
    - src/components/contact/ConsultationForm/ConsultationForm.tsx
    - src/components/contact/ConsultationForm/ConsultationForm.module.css
    - src/components/contact/ContactHub/ContactHub.tsx
    - src/components/contact/ContactHub/ContactHub.module.css
    - src/app/contact/page.tsx
  key_links:
    - "src/app/contact/page.tsx integrates ConsultationForm and ContactHub."
---

<objective>
Build an interactive consultation inquiry experience at `/contact` (INFO-03) featuring dynamic selection pills for project location, architectural status, project scope, and budget tier, paired with Haven's direct studio contact hub.

Purpose: Empowers prospective homeowners and architects to initiate high-touch consultation discussions with clear project parameters.
Output: ConsultationForm component, ContactHub component, and /contact page route.
</objective>

<execution_context>
@~/.gemini/antigravity/gsd-core/workflows/execute-plan.md
@~/.gemini/antigravity/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-informational-pages-consultation-form/05-CONTEXT.md
@.planning/phases/05-informational-pages-consultation-form/05-RESEARCH.md
@src/content/pages/contact.json
</context>

<tasks>

<task type="code">
  <name>Task 1: Build interactive ConsultationForm and ContactHub components</name>
  <files>src/components/contact/ConsultationForm/ConsultationForm.tsx, src/components/contact/ConsultationForm/ConsultationForm.module.css, src/components/contact/ContactHub/ContactHub.tsx, src/components/contact/ContactHub/ContactHub.module.css</files>
  <action>
    Implement INFO-03 (Consultation Inquiry Form & Studio Hub):
    1. Create directories `src/components/contact/ConsultationForm/` and `src/components/contact/ContactHub/`.
    2. Create `src/components/contact/ConsultationForm/ConsultationForm.module.css`:
       - `.form`: clean, expansive luxury form container with subtle borders and responsive grid.
       - `.fieldGroup`: label with uppercase tracking, helper text, and input wrapper.
       - `.pillGrid`: flexible wrap container with interactive option pills.
       - `.pillButton`: rounded pill button with transition, active state styled in signature blue `#0c388d`.
       - `.input`: minimal underline or bordered inputs styled with CSS tokens, fluid font size, and clear focus ring.
       - `.submitButton`: primary brand CTA button with hover elevation and kinetic feedback.
       - `.successCard`: bespoke executive confirmation view detailing submission received and 24–48h callback timeframe.
    3. Create `src/components/contact/ConsultationForm/ConsultationForm.tsx`:
       - Marked `"use client"`.
       - State management for:
         - `location`: `Essendon` | `Aberfeldie` | `Moonee Ponds` | `Ascot Vale` | `Other Melbourne`
         - `architecturalStatus`: `Concept Phase` | `Planning Approved` | `Drawings Ready` | `Tender Stage` | `Need Referral`
         - `scope`: `New Luxury Home` | `Renovation & Extension` | `Multi-Residential`
         - `budget`: `$1.5M - $2.5M` | `$2.5M - $4M` | `$4M - $6M` | `$6M+`
         - `name`, `email`, `phone`, `siteAddress`, `message`.
       - Client-side validation: required fields check, email format validation.
       - Success state display on valid submission with reset option.
    4. Create `src/components/contact/ContactHub/ContactHub.module.css` and `ContactHub.tsx`:
       - Cards for Phone (`0483 987 479`), Email (`info@havenconstructions.com.au`), and Studio (`21/46 Graingers Rd, West Footscray VIC 3012`).
    5. Verify with `npx tsc --noEmit`.
  </action>
  <verify>
    <automated>test -f src/components/contact/ConsultationForm/ConsultationForm.tsx && test -f src/components/contact/ContactHub/ContactHub.tsx && npx tsc --noEmit</automated>
    <fails_when>missing ConsultationForm or ContactHub components or TypeScript errors</fails_when>
  </verify>
  <done>ConsultationForm and ContactHub components built with interactive states (INFO-03).</done>
</task>

<task type="code">
  <name>Task 2: Assemble /contact page route and verify build</name>
  <files>src/app/contact/page.tsx</files>
  <action>
    Implement INFO-03 (/contact page route):
    1. Create `src/app/contact/page.tsx`:
       - Metadata: `title: "Contact & Consultation — Haven Constructions | Custom Luxury Builder"`
       - Header with `<TextReveal>`: "Let's explore your vision."
       - Renders `<ConsultationForm />` and `<ContactHub />`.
    2. Run full verification with `npx tsc --noEmit` and `npm run build`.
  </action>
  <verify>
    <automated>test -f src/app/contact/page.tsx && npx tsc --noEmit && npm run build</automated>
    <fails_when>missing contact route or build errors</fails_when>
  </verify>
  <done>Interactive /contact page assembled and verified in Next.js production build (INFO-03).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User input / Client component | Form fields submitted in browser |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-02 | Input Validation | ConsultationForm | low | mitigate | Sanitize and validate email format and required fields on client |
</threat_model>

<verification>
1. Form component: `test -f src/components/contact/ConsultationForm/ConsultationForm.tsx`
2. Contact hub: `test -f src/components/contact/ContactHub/ContactHub.tsx`
3. Contact route: `test -f src/app/contact/page.tsx`
4. Build check: `npm run build` exits 0 with /contact prerendered
</verification>

<success_criteria>
- [ ] `<ConsultationForm>` provides interactive selection pills for location, architectural progress, scope, and budget tier.
- [ ] Accessible form inputs with instant validation and luxury confirmation card state.
- [ ] `<ContactHub>` showcases direct phone, email, and studio address.
- [ ] `/contact` route builds cleanly into static bundle.
- [ ] `npm run build` succeeds with zero errors.
</success_criteria>

<output>
Create `.planning/phases/05-informational-pages-consultation-form/05-02-SUMMARY.md` when done
</output>
