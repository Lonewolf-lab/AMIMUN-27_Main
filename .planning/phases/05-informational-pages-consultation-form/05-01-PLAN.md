---
phase: 05-informational-pages-consultation-form
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/narrative/NarrativePage.tsx
  - src/components/narrative/NarrativePage.module.css
  - src/app/about/page.tsx
  - src/app/expertise/page.tsx
  - src/app/for-architects/page.tsx
autonomous: true
requirements:
  - INFO-01
  - INFO-02
user_setup: []
estimate:
  tokens: 45000
  raw_tokens: 22000
  tasks: 2
  confidence: high
must_haves:
  truths:
    - "D-01: /about page renders Haven's 3-generation family heritage, fine joinery origins, and craft philosophy."
    - "D-02: /expertise page articulates concept-to-completion precision, joinery roots, and construction discipline."
    - "D-03: /for-architects page provides tender engagement standards, buildability consultation, and architectural fidelity."
    - "'npx tsc --noEmit' and 'npm run build' compile cleanly."
  artifacts:
    - src/components/narrative/NarrativePage.tsx
    - src/components/narrative/NarrativePage.module.css
    - src/app/about/page.tsx
    - src/app/expertise/page.tsx
    - src/app/for-architects/page.tsx
  key_links:
    - "src/app/about/page.tsx uses getPageContent('about') from src/lib/content."
    - "src/app/expertise/page.tsx uses getPageContent('expertise') from src/lib/content."
    - "src/app/for-architects/page.tsx uses getPageContent('for-architects') from src/lib/content."
---

<objective>
Build the core informational and brand storytelling pages: `/about` (INFO-01), `/expertise` (INFO-02), and `/for-architects` (INFO-02) using modular narrative layout components, kinetic typography, and high-resolution architectural imagery.

Purpose: Establishes Haven Constructions' authority, heritage, and architectural alignment credentials for prospective clients and design partners.
Output: NarrativePage component, /about route, /expertise route, /for-architects route.
</objective>

<execution_context>
@~/.gemini/antigravity/gsd-core/workflows/execute-plan.md
@~/.gemini/antigravity/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-informational-pages-consultation-form/05-CONTEXT.md
@.planning/phases/05-informational-pages-consultation-form/05-RESEARCH.md
@src/content/pages/about.json
@src/content/pages/expertise.json
@src/content/pages/for-architects.json
@src/lib/content.ts
</context>

<tasks>

<task type="code">
  <name>Task 1: Build modular NarrativePage layout component</name>
  <files>src/components/narrative/NarrativePage.tsx, src/components/narrative/NarrativePage.module.css</files>
  <action>
    Create reusable narrative page presentation:
    1. Create `src/components/narrative/NarrativePage.module.css`:
       - `.container`: `max-width: var(--max-container-width); margin: 0 auto; padding: calc(var(--navigation-height) + var(--space-xl)) var(--space-gutter) var(--space-section-large);`
       - `.header`: page label (`--color-brand-primary`), large `<TextReveal>` heading (`--step-5`), intro text (`--step-1`, `--color-text-secondary`).
       - `.fullBleedImage`: aspect-ratio `16/10` with subtle border-radius and responsive Next.js Image.
       - `.sectionBlock`: section subheading, heading statement (`--step-3`), and 2-column or centered narrative content (`--step-0`, line-height `1.7`).
       - `.threeColumnGrid`: responsive 3-column image gallery with `4/5` or `1/1` aspect ratios.
       - `.ctaSection`: bottom consultation invitation leading to `/contact`.
    2. Create `src/components/narrative/NarrativePage.tsx`:
       - Accepts `page: PageContent` prop.
       - Iterates through `page.blocks`, rendering `section-intro`, `image` (full width), and `image-three-column` seamlessly with `<TextReveal>` kinetic headings.
    3. Verify with `npx tsc --noEmit`.
  </action>
  <verify>
    <automated>test -f src/components/narrative/NarrativePage.tsx && test -f src/components/narrative/NarrativePage.module.css && npx tsc --noEmit</automated>
    <fails_when>missing NarrativePage files or TypeScript errors</fails_when>
  </verify>
  <done>Modular NarrativePage component built and validated.</done>
</task>

<task type="code">
  <name>Task 2: Assemble /about, /expertise, and /for-architects pages</name>
  <files>src/app/about/page.tsx, src/app/expertise/page.tsx, src/app/for-architects/page.tsx</files>
  <action>
    Implement INFO-01 and INFO-02:
    1. Create `src/app/about/page.tsx`:
       - Metadata: `title: "About Haven — Three Generations of Bespoke Craftsmanship"`
       - Loads `getPageContent("about")` and renders `<NarrativePage page={page} />`.
    2. Create `src/app/expertise/page.tsx`:
       - Metadata: `title: "Expertise — Precision Residential Construction & Joinery | Haven"`
       - Loads `getPageContent("expertise")` and renders `<NarrativePage page={page} />`.
    3. Create `src/app/for-architects/page.tsx`:
       - Metadata: `title: "For Architects — Collaborative Luxury Construction | Haven"`
       - Loads `getPageContent("for-architects")` and renders `<NarrativePage page={page} />`.
    4. Verify with `npx tsc --noEmit` and `npm run build`.
  </action>
  <verify>
    <automated>test -f src/app/about/page.tsx && test -f src/app/expertise/page.tsx && test -f src/app/for-architects/page.tsx && npx tsc --noEmit && npm run build</automated>
    <fails_when>missing page routes or build failures</fails_when>
  </verify>
  <done>/about, /expertise, and /for-architects routes pre-rendered and verified (INFO-01, INFO-02).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Content parser / DOM | HTML string from page.content rendered via dangerouslySetInnerHTML |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-01 | Tampering / XSS | dangerouslySetInnerHTML | low | mitigate | Content originates strictly from static local JSON bundled in repository |
</threat_model>

<verification>
1. About route: `test -f src/app/about/page.tsx`
2. Expertise route: `test -f src/app/expertise/page.tsx`
3. For Architects route: `test -f src/app/for-architects/page.tsx`
4. Build check: `npm run build` exits 0 with all three routes prerendered
</verification>

<success_criteria>
- [ ] `<NarrativePage>` layout component handles section-intro, full images, and 3-column galleries.
- [ ] `/about` displays Haven's 3-generation heritage, leadership profile, and craft values (INFO-01).
- [ ] `/expertise` details construction discipline and fine joinery origins (INFO-02).
- [ ] `/for-architects` articulates tender alignment and buildability collaboration (INFO-02).
- [ ] `npm run build` generates all static routes with 0 errors.
</success_criteria>

<output>
Create `.planning/phases/05-informational-pages-consultation-form/05-01-SUMMARY.md` when done
</output>
