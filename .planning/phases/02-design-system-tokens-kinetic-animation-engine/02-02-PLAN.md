---
phase: 02-design-system-tokens-kinetic-animation-engine
plan: 02
type: execute
wave: 2
depends_on:
  - "02-01"
files_modified:
  - src/components/animations/TextReveal.tsx
  - src/components/animations/TextReveal.module.css
  - src/app/page.tsx
autonomous: true
requirements:
  - DS-03
user_setup: []
estimate:
  tokens: 45000
  raw_tokens: 22000
  tasks: 2
  confidence: high
must_haves:
  truths:
    - "D-03: Reusable <TextReveal> component splits text into words with word-parent and word-child spans, triggered on viewport entry via IntersectionObserver."
    - "D-04: Word animation uses exact 1.4s cubic-bezier(0.23, 1, 0.32, 1) transition with 20ms stagger per word."
    - "D-05: <TextReveal> supports polymorphic 'as' prop (h1, h2, h3, h4, h5, h6, p, span, div) and 'baseDelay' offset prop."
    - "'npm run build' and 'npx tsc --noEmit' pass cleanly."
  artifacts:
    - src/components/animations/TextReveal.tsx
    - src/components/animations/TextReveal.module.css
    - src/app/page.tsx
  key_links:
    - "src/components/animations/TextReveal.tsx imports styles from TextReveal.module.css."
    - "src/app/page.tsx imports and renders TextReveal components across multiple headings."
---

<objective>
Build the reusable polymorphic `<TextReveal>` kinetic animation component (D-03, D-04, D-05) matching the legacy site's `[animation="text-reveal"]` staggered word transition, and integrate it into `src/app/page.tsx`.

Purpose: Delivers the signature luxury text entrance motion for titles and statements across all present and future pages with zero runtime JS animation overhead.
Output: `<TextReveal>` component, `TextReveal.module.css`, and animated demo homepage.
</objective>

<execution_context>
@~/.gemini/antigravity/gsd-core/workflows/execute-plan.md
@~/.gemini/antigravity/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-design-system-tokens-kinetic-animation-engine/02-CONTEXT.md
@.planning/phases/02-design-system-tokens-kinetic-animation-engine/02-RESEARCH.md
@legacy_codebase/styles.css
</context>

<tasks>

<task type="code">
  <name>Task 1: Build TextReveal component and CSS module</name>
  <files>src/components/animations/TextReveal.tsx, src/components/animations/TextReveal.module.css</files>
  <action>
    Implement D-03 (hybrid CSS + IntersectionObserver architecture), D-04 (exact 1.4s cubic-bezier and 20ms stagger timing), and D-05 (polymorphic 'as' prop and 'baseDelay'):
    1. Create directory `src/components/animations/`.
    2. Create `src/components/animations/TextReveal.module.css`:
       - `.revealContainer`: `display: inline-block;`
       - `.wordParent`: `display: inline-block; overflow: clip; vertical-align: top; margin-right: 0.28em;`
       - `.wordChild`: `display: inline-block; transform: translateY(105%); transition: transform 1.4s cubic-bezier(0.23, 1, 0.32, 1); will-change: transform;`
       - `.active .wordChild`: `transform: translateY(0%);`
    3. Create `src/components/animations/TextReveal.tsx`:
       - Mark with `"use client"`.
       - Accept props: `children: string`, `as?: "h1" | "h2" | "h3" | "h4" | "h5" | "h6" | "p" | "span" | "div"`, `className?: string`, `baseDelay?: number`.
       - Attach `useRef<HTMLElement>` and `IntersectionObserver` with threshold 0.15 and trigger-once behavior.
       - Split `children` by spaces, wrapping each word in `.wordParent` and `.wordChild`.
       - Assign dynamic `transitionDelay: (baseDelay + index * 20) + "ms"`.
    4. Verify with `npx tsc --noEmit`.
  </action>
  <verify>
    <automated>test -f src/components/animations/TextReveal.tsx && test -f src/components/animations/TextReveal.module.css && npx tsc --noEmit</automated>
    <fails_when>missing TextReveal files or TypeScript compilation failure</fails_when>
  </verify>
  <done>`<TextReveal>` component and CSS module are created with polymorphic props and tested via TypeScript compiler.</done>
</task>

<task type="code">
  <name>Task 2: Integrate TextReveal into homepage and verify production build</name>
  <files>src/app/page.tsx</files>
  <action>
    Integrate `<TextReveal>` into `src/app/page.tsx`:
    1. Import `<TextReveal>` from `@/components/animations/TextReveal`.
    2. Wrap the main hero heading "HAVEN" in `<TextReveal as="h1" baseDelay={100}>HAVEN</TextReveal>`.
    3. Wrap the brand statement in `<TextReveal as="p" className="text--size-l" baseDelay={250}>...</TextReveal>`.
    4. Wrap section heading "Portfolio Showcase" in `<TextReveal as="h2">Portfolio Showcase</TextReveal>`.
    5. Run `npx tsc --noEmit` and `npm run build` to ensure the entire page with smooth scrolling and animations compiles without errors.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm run build</automated>
    <fails_when>non-zero exit code or compilation error in production build</fails_when>
  </verify>
  <done>Homepage integrates `<TextReveal>` on hero and section titles and builds cleanly in Next.js.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client DOM | TextReveal splits text strings into child spans in browser runtime |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-02 | Cross-Site Scripting (XSS) | TextReveal | low | mitigate | Render word tokens as standard React text nodes (auto-escaped) rather than innerHTML |
</threat_model>

<verification>
1. Component files: `test -f src/components/animations/TextReveal.tsx`
2. TypeScript compilation: `npx tsc --noEmit`
3. Production build: `npm run build` exits 0
</verification>

<success_criteria>
- [ ] `<TextReveal>` component built with polymorphic `as` prop (D-03, D-05).
- [ ] 1.4s `cubic-bezier(0.23, 1, 0.32, 1)` transition with 20ms stagger implemented in CSS (D-04).
- [ ] Homepage demonstrates `<TextReveal>` with clean `npm run build`.
</success_criteria>

<output>
Create `.planning/phases/02-design-system-tokens-kinetic-animation-engine/02-02-SUMMARY.md` when done
</output>
