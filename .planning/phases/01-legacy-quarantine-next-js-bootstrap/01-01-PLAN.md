---
phase: 01-legacy-quarantine-next-js-bootstrap
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - legacy_codebase/
  - package.json
  - tsconfig.json
  - next.config.ts
  - src/app/layout.tsx
  - src/app/page.tsx
  - src/styles/tokens.css
  - src/styles/globals.css
  - src/styles/reset.css
autonomous: true
requirements:
  - LEG-01
  - LEG-02
user_setup: []
estimate:
  tokens: 45000
  raw_tokens: 22000
  tasks: 2
  confidence: high
must_haves:
  truths:
    - "D-01: Cloned files (index.html, server.py, styles.css, _next/, icons/) are moved to legacy_codebase/."
    - "D-02: 'npm run legacy' script is defined in package.json to run python3 legacy_codebase/server.py 8080."
    - "D-05: Standard Next.js 15 App Router workspace structure is initialized under src/ with React 19 and TypeScript."
    - "D-06: Styling stack uses Vanilla CSS tokens (tokens.css, globals.css) and CSS Modules without Tailwind CSS."
    - "'npm run build' succeeds at the workspace root with zero errors."
  artifacts:
    - legacy_codebase/index.html
    - legacy_codebase/server.py
    - package.json
    - tsconfig.json
    - next.config.ts
    - src/app/layout.tsx
    - src/app/page.tsx
    - src/styles/tokens.css
    - src/styles/globals.css
  key_links:
    - "package.json scripts integrate Next.js commands ('dev', 'build', 'start', 'lint') and 'legacy' proxy command."
    - "src/app/layout.tsx imports src/styles/globals.css."
---

<objective>
Quarantine the monolithic 50MB cloned bundle into `legacy_codebase/` (D-01, D-02) and bootstrap a clean, modern Next.js 15 (App Router) + React 19 + TypeScript workspace at the project root under `src/` (D-05) with Vanilla CSS tokens and CSS Modules (D-06, no Tailwind CSS).

Purpose: Restores editor responsiveness, decouples the codebase from monolithic legacy files, and establishes a clean, high-performance foundation for componentization while keeping legacy reference intact.
Output: Isolated `legacy_codebase/` directory and building Next.js 15 project at root.
</objective>

<execution_context>
@~/.gemini/antigravity/gsd-core/workflows/execute-plan.md
@~/.gemini/antigravity/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-legacy-quarantine-next-js-bootstrap/01-CONTEXT.md
@.planning/phases/01-legacy-quarantine-next-js-bootstrap/01-RESEARCH.md
@GEMINI.md
</context>

<tasks>

<task type="code">
  <name>Task 1: Quarantine monolithic legacy assets into legacy_codebase/</name>
  <files>legacy_codebase/</files>
  <action>
    Implement D-01 (full quarantine of legacy files):
    Create directory `legacy_codebase/` if it does not already exist.
    Move the legacy cloned assets from the workspace root into `legacy_codebase/`:
    - `index.html` (50MB monolithic clone) -> `legacy_codebase/index.html`
    - `server.py` -> `legacy_codebase/server.py`
    - `styles.css` -> `legacy_codebase/styles.css`
    - `_next/` -> `legacy_codebase/_next/`
    - `icons/` -> `legacy_codebase/icons/`

    CRITICAL SAFEGUARD: Do NOT touch, move, or modify `.git/`, `.planning/`, or `GEMINI.md`.
    Verify that `legacy_codebase/index.html` exists and `index.html` no longer exists at root.
  </action>
  <verify>
    <automated>test -d legacy_codebase && test -f legacy_codebase/index.html && test -f legacy_codebase/server.py && test -d legacy_codebase/_next && test ! -f ./index.html && test ! -f ./server.py</automated>
    <fails_when>non-zero exit code or missing files in legacy_codebase/</fails_when>
  </verify>
  <done>All legacy cloned files reside inside `legacy_codebase/` per D-01 and the root directory is clean of legacy clutter.</done>
</task>

<task type="code">
  <name>Task 2: Bootstrap Next.js 15 App Router workspace with Vanilla CSS tokens</name>
  <files>package.json, tsconfig.json, next.config.ts, src/app/layout.tsx, src/app/page.tsx, src/styles/tokens.css, src/styles/globals.css, src/styles/reset.css</files>
  <action>
    Implement D-02 (npm run legacy script), D-05 (Next.js 15 App Router directory layout under src/), and D-06 (Vanilla CSS tokens and CSS Modules, strictly no Tailwind):
    1. Configure `package.json` with dependencies:
       - `next`: `^15.2.0` (or latest 15.x)
       - `react`: `^19.0.0`
       - `react-dom`: `^19.0.0`
       - devDependencies: `typescript`, `@types/react`, `@types/react-dom`, `@types/node`, `eslint`, `eslint-config-next`
    2. Add standard scripts to `package.json`:
       - `"dev": "next dev"`
       - `"build": "next build"`
       - `"start": "next start"`
       - `"lint": "next lint"`
       - `"legacy": "python3 legacy_codebase/server.py 8080"` (Per Decision D-02)
    3. Install dependencies using `npm install`.
    4. Configure `tsconfig.json` with `@/*` path alias mapped to `./src/*`, `target: ES2022`, `moduleResolution: bundler`, `strict: true`.
    5. Configure `next.config.ts` enabling React strict mode.
    6. Per D-06, create `src/styles/tokens.css` carrying fluid clamp typography scales (`--step--2` through `--step-10`) and fluid spacing scales (`--space-3xs` through `--space-5xl`) extracted from `legacy_codebase/styles.css`.
    7. Create `src/styles/reset.css` for clean luxury layout normalization.
    8. Create `src/styles/globals.css` importing `tokens.css` and `reset.css`.
    9. Per D-05, create `src/app/layout.tsx` setting up HTML document, title metadata ("Haven Constructions — Luxury Home Builder"), and importing `globals.css`.
    10. Create a clean starter `src/app/page.tsx` rendering a placeholder luxury hero container.
    11. Verify the project builds cleanly with `npm run build`.
  </action>
  <verify>
    <automated>npm run build</automated>
    <fails_when>non-zero exit code or TypeScript/ESLint compiler errors</fails_when>
  </verify>
  <done>`npm run build` succeeds cleanly at root and D-02, D-05, D-06 are fulfilled.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Legacy code boundary | Legacy cloned bundle is isolated in `legacy_codebase/` and cannot execute in Next.js build |
| File system boundary | Next.js source files strictly reside in `src/` |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-01 | Information Disclosure | `package.json` / Next build | low | mitigate | Do not expose secret credentials or environment tokens in public config |
| T-01-02 | Denial of Service | File system build | medium | mitigate | Quarantine 50MB monolithic `index.html` so Next.js bundler does not parse it |
| T-01-SC | Tampering | npm dependencies | high | mitigate | Install only verified, official npm packages (`next`, `react`, `react-dom`, `typescript`) |
</threat_model>

<verification>
1. Quarantined legacy assets: `test -f legacy_codebase/index.html && test ! -f index.html`
2. Next.js build verification: `npm run build` exits 0.
3. TypeScript verification: `npx tsc --noEmit` exits 0.
</verification>

<success_criteria>
- [ ] Monolithic cloned files are relocated into `legacy_codebase/` (D-01).
- [ ] Next.js 15, React 19, TypeScript workspace is initialized under `src/` (D-05).
- [ ] No Tailwind CSS is installed; Vanilla CSS tokens are configured (D-06).
- [ ] `npm run build` compiles with zero errors.
- [ ] `npm run legacy` is wired for port 8080 comparison (D-02).
</success_criteria>

<output>
Create `.planning/phases/01-legacy-quarantine-next-js-bootstrap/01-01-SUMMARY.md` when done
</output>
