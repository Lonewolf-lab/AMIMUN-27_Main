---
phase: 01-legacy-quarantine-next-js-bootstrap
plan: 02
type: execute
wave: 2
depends_on:
  - "01-01"
files_modified:
  - public/fonts/suisse.woff2
  - src/app/fonts.ts
  - src/types/content.ts
  - src/lib/content.ts
  - src/content/projects/ascot-residence.json
  - src/content/projects/calibre.json
  - src/content/projects/obsidian.json
  - src/content/projects/small-house.json
  - src/content/pages/about.json
  - src/content/pages/expertise.json
  - src/content/pages/contact.json
  - src/content/pages/for-architects.json
autonomous: true
requirements:
  - LEG-03
user_setup: []
estimate:
  tokens: 45000
  raw_tokens: 22000
  tasks: 2
  confidence: high
must_haves:
  truths:
    - "D-03: Project case studies (ascot-residence, calibre, obsidian, small-house) and static pages (about, expertise, contact) are stored as JSON files in src/content/."
    - "D-04: Lightweight TypeScript interfaces in src/types/content.ts and getter helpers (getAllProjects, getProjectBySlug) in src/lib/content.ts provide type-safe content access."
    - "D-07: Legacy font c42cb4950679bdba-s.p.woff2 is copied to public/fonts/suisse.woff2 and loaded via next/font/local into CSS variable --font-suisse."
    - "'npm run build' and 'npx tsc --noEmit' pass cleanly."
  artifacts:
    - public/fonts/suisse.woff2
    - src/app/fonts.ts
    - src/types/content.ts
    - src/lib/content.ts
    - src/content/projects/ascot-residence.json
    - src/content/projects/calibre.json
    - src/content/projects/obsidian.json
    - src/content/projects/small-house.json
    - src/content/pages/about.json
    - src/content/pages/expertise.json
    - src/content/pages/contact.json
  key_links:
    - "src/app/fonts.ts exports suisseFont configured with variable '--font-suisse'."
    - "src/lib/content.ts imports project JSON files from src/content/projects/ and exports getAllProjects and getProjectBySlug."
---

<objective>
Configure the local luxury typography system using `next/font/local` with the quarantined legacy `.woff2` font (D-07), extract structured content JSONs from the legacy Next.js data bundle into `src/content/projects/` and `src/content/pages/` (D-03), and implement type-safe content loaders in `src/lib/content.ts` with lightweight TypeScript interfaces in `src/types/content.ts` (D-04).

Purpose: Provides the data layer and typographic foundation that will power portfolio templates in Phase 4 and informational pages in Phase 5, completely decoupling future components from legacy bundle files.
Output: Local font asset, font configuration module, typed content JSON files, and content getter utilities.
</objective>

<execution_context>
@~/.gemini/antigravity/gsd-core/workflows/execute-plan.md
@~/.gemini/antigravity/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-legacy-quarantine-next-js-bootstrap/01-CONTEXT.md
@.planning/phases/01-legacy-quarantine-next-js-bootstrap/01-RESEARCH.md
@legacy_codebase/_next/data/G9U8tHBk031x0vfc7LJ4H/projects/ascot-residence.json
@legacy_codebase/_next/data/G9U8tHBk031x0vfc7LJ4H/projects/calibre.json
@legacy_codebase/_next/data/G9U8tHBk031x0vfc7LJ4H/projects/obsidian.json
@legacy_codebase/_next/data/G9U8tHBk031x0vfc7LJ4H/projects/small-house.json
</context>

<tasks>

<task type="code">
  <name>Task 1: Setup luxury Suisse typography with next/font/local</name>
  <files>public/fonts/suisse.woff2, src/app/fonts.ts, src/app/layout.tsx</files>
  <action>
    Implement D-07 (exact typography loading via next/font/local):
    1. Create directory `public/fonts/`.
    2. Copy `legacy_codebase/_next/static/media/c42cb4950679bdba-s.p.woff2` to `public/fonts/suisse.woff2`.
    3. Create `src/app/fonts.ts` using `next/font/local`:
       ```typescript
       import localFont from 'next/font/local';

       export const suisseFont = localFont({
         src: [
           {
             path: '../../public/fonts/suisse.woff2',
             weight: '400',
             style: 'normal',
           },
         ],
         variable: '--font-suisse',
         display: 'swap',
       });
       ```
    4. Update `src/app/layout.tsx` to add `${suisseFont.variable}` to the root `<html>` or `<body>` element className.
    5. In `src/styles/tokens.css` or `globals.css`, set `--font-family-text: var(--font-suisse), sans-serif;` so that body text adopts the Suisse font family.
    6. Verify with `npx tsc --noEmit`.
  </action>
  <verify>
    <automated>test -f public/fonts/suisse.woff2 && test -f src/app/fonts.ts && npx tsc --noEmit</automated>
    <fails_when>missing font file, missing fonts.ts, or TypeScript compiler errors</fails_when>
  </verify>
  <done>Suisse luxury font is copied to public/fonts/ and wired via next/font/local into CSS custom property `--font-suisse` per D-07.</done>
</task>

<task type="code">
  <name>Task 2: Extract structured project case studies and page data into typed content modules</name>
  <files>src/content/projects/ascot-residence.json, src/content/projects/calibre.json, src/content/projects/obsidian.json, src/content/projects/small-house.json, src/content/pages/about.json, src/content/pages/expertise.json, src/content/pages/contact.json, src/content/pages/for-architects.json, src/types/content.ts, src/lib/content.ts</files>
  <action>
    Implement D-03 (hybrid content storage with JSON files) and D-04 (lightweight TypeScript interfaces and loader functions without Zod):
    1. Create `src/content/projects/` and `src/content/pages/` directories.
    2. Extract clean project payloads from `legacy_codebase/_next/data/G9U8tHBk031x0vfc7LJ4H/`:
       - Extract Ascot Residence data to `src/content/projects/ascot-residence.json`
       - Extract Calibre data to `src/content/projects/calibre.json`
       - Extract Obsidian data to `src/content/projects/obsidian.json`
       - Extract Small House data to `src/content/projects/small-house.json`
       Include slug, title, heading, content, details (Name, Status, Location, Architect), mainImage, nextPost, and gallery blocks.
    3. Extract static page copy to `src/content/pages/`:
       - `about.json`
       - `expertise.json`
       - `contact.json`
       - `for-architects.json`
    4. Create `src/types/content.ts` defining:
       - `ProjectDetail`: `{ label: string; value: string }`
       - `ImageSource`: `{ sourceUrl: string; alt?: string; width?: number | string; height?: number | string; blurPlaceholder?: string }`
       - `Project`: `{ slug: string; title: string; heading: string; content: string; details: ProjectDetail[]; mainImage: ImageSource; nextPost?: any; blocks: any[] }`
       - `PageContent`: interface for generic page layouts and blocks.
    5. Create `src/lib/content.ts` providing typed loader functions:
       - `getAllProjects(): Project[]`
       - `getProjectBySlug(slug: string): Project | undefined`
       - `getPageContent(slug: string): any`
    6. Verify that all extracted JSON files are valid and load cleanly without TypeScript errors (`npx tsc --noEmit`).
    7. Run `npm run build` to confirm full production build compiles.
  </action>
  <verify>
    <automated>test -f src/content/projects/ascot-residence.json && test -f src/content/projects/calibre.json && test -f src/content/projects/obsidian.json && test -f src/content/projects/small-house.json && test -f src/lib/content.ts && test -f src/types/content.ts && npx tsc --noEmit && npm run build</automated>
    <fails_when>missing content files or TypeScript compilation failure</fails_when>
  </verify>
  <done>All 4 luxury project case studies and static page contents are extracted into `src/content/` per D-03 with strong TypeScript definitions and tested getter utilities per D-04.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Content boundary | Static JSON files are read from local repository and bundled at build time |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-03 | Information Disclosure | `src/content/` | low | accept | Showcase content is public marketing data meant for indexing |
| T-01-04 | Tampering | Content JSON parsing | low | mitigate | Strict TypeScript typing and static imports prevent unexpected runtime structure |
</threat_model>

<verification>
1. Font existence: `test -f public/fonts/suisse.woff2`
2. Content existence: Check all 4 project JSON files and page JSON files exist in `src/content/`
3. TypeScript compiler: `npx tsc --noEmit` passes with 0 errors
4. Production build: `npm run build` passes with 0 errors
</verification>

<success_criteria>
- [ ] Suisse font asset is copied and loaded via `next/font/local` (D-07).
- [ ] Ascot Residence, Calibre, Obsidian, and Small House JSON files are extracted in `src/content/projects/` (D-03).
- [ ] Static page data JSONs are extracted in `src/content/pages/` (D-03).
- [ ] `src/types/content.ts` and `src/lib/content.ts` are fully typed and tested (D-04).
- [ ] `npm run build` succeeds cleanly.
</success_criteria>

<output>
Create `.planning/phases/01-legacy-quarantine-next-js-bootstrap/01-02-SUMMARY.md` when done
</output>
