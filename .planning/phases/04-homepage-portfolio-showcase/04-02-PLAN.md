---
phase: 04-homepage-portfolio-showcase
plan: 02
type: execute
wave: 2
depends_on:
  - "04-01"
files_modified:
  - src/app/projects/[slug]/page.tsx
  - src/components/portfolio/ProjectDetail.tsx
  - src/components/portfolio/ProjectDetail.module.css
  - src/app/projects/page.tsx
  - src/components/portfolio/PortfolioArchive.tsx
  - src/components/portfolio/PortfolioArchive.module.css
autonomous: true
requirements:
  - PORT-02
  - PORT-03
user_setup: []
estimate:
  tokens: 45000
  raw_tokens: 22000
  tasks: 2
  confidence: high
must_haves:
  truths:
    - "D-02: /projects/[slug] route generates static pages for ascot-residence, calibre, obsidian, and small-house via generateStaticParams()."
    - "D-03: Project case study layout renders metadata table, architectural narrative, image galleries, and next-project navigation."
    - "D-04: /projects index page provides interactive category/suburb filters and responsive project card grid."
    - "'npm run build' and 'npx tsc --noEmit' pass cleanly with zero errors."
  artifacts:
    - src/app/projects/[slug]/page.tsx
    - src/components/portfolio/ProjectDetail.tsx
    - src/components/portfolio/ProjectDetail.module.css
    - src/app/projects/page.tsx
    - src/components/portfolio/PortfolioArchive.tsx
    - src/components/portfolio/PortfolioArchive.module.css
  key_links:
    - "src/app/projects/[slug]/page.tsx uses getProjectBySlug and getAllProjects from src/lib/content."
    - "src/app/projects/page.tsx uses getAllProjects from src/lib/content."
---

<objective>
Build dynamic project case study pages at `/projects/[slug]` (PORT-02, D-02, D-03) and the comprehensive portfolio archive at `/projects` (PORT-03, D-04) with category filters and interactive hover states.

Purpose: Enables prospective clients and architects to explore individual bespoke homes in depth with high-resolution imagery and architectural attributions.
Output: Dynamic case study route, portfolio index route, and supporting components.
</objective>

<execution_context>
@~/.gemini/antigravity/gsd-core/workflows/execute-plan.md
@~/.gemini/antigravity/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-homepage-portfolio-showcase/04-CONTEXT.md
@.planning/phases/04-homepage-portfolio-showcase/04-RESEARCH.md
@src/content/projects/
@src/lib/content.ts
@src/types/content.ts
</context>

<tasks>

<task type="code">
  <name>Task 1: Build dynamic project case study pages (/projects/[slug])</name>
  <files>src/app/projects/[slug]/page.tsx, src/components/portfolio/ProjectDetail.tsx, src/components/portfolio/ProjectDetail.module.css</files>
  <action>
    Implement PORT-02, D-02, and D-03:
    1. Create directory `src/components/portfolio/` and `src/app/projects/[slug]/`.
    2. Create `src/components/portfolio/ProjectDetail.module.css`:
       - `.container`: `max-width: var(--max-container-width); margin: 0 auto; padding: calc(var(--navigation-height) + var(--space-xl)) var(--space-gutter) var(--space-section-large);`
       - `.heroSection`: full-width layout with project title (`h1`), heading statement, and full-bleed main image.
       - `.metaGrid`: metadata bar with Name, Status, Location, and Architect in responsive 4-column layout.
       - `.contentSection`: narrative text describing design resolution, spatial flow, and material palettes.
       - `.gallerySection`: renders image blocks (full-bleed, 2-column, 3-column) with aspect ratio containment and Next.js Image.
       - `.nextProjectCard`: transition teaser at the bottom linking to the next case study.
    3. Create `src/components/portfolio/ProjectDetail.tsx`:
       - Props: `project: ProjectContent`.
       - Renders complete case study view matching design tokens and typography scales.
    4. Create `src/app/projects/[slug]/page.tsx`:
       - `generateStaticParams()` returning `{ slug: project.slug }` for all projects from `getAllProjects()`.
       - `generateMetadata()` setting page title e.g. `"${project.title} — Haven Constructions"`.
       - Calls `notFound()` if slug does not exist.
    5. Verify with `npx tsc --noEmit`.
  </action>
  <verify>
    <automated>test -f src/app/projects/[slug]/page.tsx && test -f src/components/portfolio/ProjectDetail.tsx && npx tsc --noEmit</automated>
    <fails_when>missing dynamic route files or TypeScript compilation failure</fails_when>
  </verify>
  <done>Dynamic project case study page built with static generation for all 4 residences (PORT-02).</done>
</task>

<task type="code">
  <name>Task 2: Build portfolio index page (/projects) with suburb filters</name>
  <files>src/app/projects/page.tsx, src/components/portfolio/PortfolioArchive.tsx, src/components/portfolio/PortfolioArchive.module.css</files>
  <action>
    Implement PORT-03 and D-04:
    1. Create `src/components/portfolio/PortfolioArchive.module.css`:
       - `.container`: `max-width: var(--max-container-width); margin: 0 auto; padding: calc(var(--navigation-height) + var(--space-xl)) var(--space-gutter) var(--space-section-large);`
       - `.header`: page title "Portfolio", subtitle, and filter bar.
       - `.filterBar`: flex container with filter pills (`All`, `Essendon`, `Aberfeldie`, `Moonee Ponds`, `Ascot Vale`).
       - `.filterButton`: interactive pill button with active state styling.
       - `.grid`: responsive 2-column or 3-column project cards.
       - `.card`: luxury project card with image container, hover zoom, title, location, and year.
    2. Create `src/components/portfolio/PortfolioArchive.tsx`:
       - Mark with `"use client"`.
       - Filter state management based on selected suburb.
       - Renders filtered list of project cards linking to `/projects/[slug]`.
    3. Create `src/app/projects/page.tsx`:
       - Metadata: `title: "Portfolio — Haven Constructions | Custom Luxury Residences"`.
       - Loads `getAllProjects()` and renders `<PortfolioArchive projects={projects} />`.
    4. Verify with `npx tsc --noEmit` and `npm run build`.
  </action>
  <verify>
    <automated>test -f src/app/projects/page.tsx && test -f src/components/portfolio/PortfolioArchive.tsx && npx tsc --noEmit && npm run build</automated>
    <fails_when>missing portfolio archive files or Next.js build errors</fails_when>
  </verify>
  <done>Portfolio archive page built with interactive suburb filters and builds cleanly (PORT-03).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Content params / Route | Dynamic route slug parameter passed from URL to content loader |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-02 | Information Disclosure / Path Traversal | getProjectBySlug | low | mitigate | Validate slug matches known project slugs and return notFound() if not found |
</threat_model>

<verification>
1. Case study route: `test -f src/app/projects/[slug]/page.tsx`
2. Archive route: `test -f src/app/projects/page.tsx`
3. TypeScript validation: `npx tsc --noEmit`
4. Static export build: `npm run build` exits 0 and pre-renders /projects and all /projects/[slug] paths
</verification>

<success_criteria>
- [ ] Dynamic `/projects/[slug]` route generates static pages for all 4 projects (PORT-02).
- [ ] High-res galleries and project detail tables render accurately (PORT-02).
- [ ] Portfolio index `/projects` provides interactive suburb filtering (PORT-03).
- [ ] `npm run build` compiles cleanly with all routes pre-rendered.
</success_criteria>

<output>
Create `.planning/phases/04-homepage-portfolio-showcase/04-02-SUMMARY.md` when done
</output>
