---
phase: 03-core-navigation-layout-shell
plan: 02
type: execute
wave: 2
depends_on:
  - "03-01"
files_modified:
  - src/components/layout/Footer/Footer.tsx
  - src/components/layout/Footer/Footer.module.css
  - src/app/layout.tsx
autonomous: true
requirements:
  - NAV-03
user_setup: []
estimate:
  tokens: 45000
  raw_tokens: 22000
  tasks: 2
  confidence: high
must_haves:
  truths:
    - "D-06: Footer component is styled with luxury dark aesthetic and displays the Bespoke Luxury dot heading."
    - "D-07: Footer explicitly lists Melbourne service suburbs (Essendon, Aberfeldie, Moonee Ponds, Ascot Vale) and accreditation."
    - "D-08: Footer features giant architectural HAVEN SVG vector backdrop and Calibre project preview card."
    - "D-09: Root layout (src/app/layout.tsx) mounts Header and Footer seamlessly around page children."
    - "'npm run build' and 'npx tsc --noEmit' pass cleanly with zero errors."
  artifacts:
    - src/components/layout/Footer/Footer.tsx
    - src/components/layout/Footer/Footer.module.css
    - src/app/layout.tsx
  key_links:
    - "src/app/layout.tsx mounts Header at top and Footer at bottom inside SmoothScrollProvider."
    - "src/components/layout/Footer/Footer.tsx imports styles from Footer.module.css and links to /projects/calibre."
---

<objective>
Build the comprehensive luxury `<Footer>` component (NAV-03, D-06, D-07, D-08) with accreditation badges, Melbourne suburb tags, giant HAVEN backdrop SVG, and project teaser, and mount `<Header>` and `<Footer>` into `src/app/layout.tsx` (D-09).

Purpose: Completes the global persistent luxury shell framing every page with architectural elegance and regional Melbourne builder credentials.
Output: Footer component, Footer CSS module, and updated root layout.
</objective>

<execution_context>
@~/.gemini/antigravity/gsd-core/workflows/execute-plan.md
@~/.gemini/antigravity/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-core-navigation-layout-shell/03-CONTEXT.md
@.planning/phases/03-core-navigation-layout-shell/03-RESEARCH.md
@legacy_codebase/styles.css
@legacy_codebase/index.html
</context>

<tasks>

<task type="code">
  <name>Task 1: Build comprehensive luxury Footer component</name>
  <files>src/components/layout/Footer/Footer.tsx, src/components/layout/Footer/Footer.module.css</files>
  <action>
    Implement NAV-03, D-06, D-07, and D-08:
    1. Create directory `src/components/layout/Footer/`.
    2. Create `src/components/layout/Footer/Footer.module.css`:
       - `.footer`: `position: relative; background-color: var(--color-background-dark, #121212); color: #ffffff; padding: var(--space-3xl) var(--space-gutter) var(--space-xl); overflow: hidden;`
       - `.gridContainer`: `max-width: var(--max-container-width); margin: 0 auto; display: flex; flex-direction: column; gap: var(--space-2xl); position: relative; z-index: 2;`
       - `.topBar`: `display: flex; justify-content: space-between; align-items: flex-start; flex-wrap: wrap; gap: var(--space-l);`
       - `.dotHeading`: `display: inline-flex; align-items: center; gap: 8px; font-size: var(--step-0); text-transform: uppercase; letter-spacing: 0.05em; color: rgba(255, 255, 255, 0.7);`
       - `.dot`: `width: 6px; height: 6px; border-radius: 50%; background-color: #2e7d32;`
       - `.navColumns`: `display: flex; gap: var(--space-2xl); flex-wrap: wrap;`
       - `.navColumn`: `display: flex; flex-direction: column; gap: var(--space-xs);`
       - `.navLink`: `color: #ffffff; text-decoration: none; font-size: var(--step-0); transition: opacity 0.2s;`
       - `.navLink:hover`: `opacity: 0.7;`
       - `.giantLogoWrapper`: `width: 100%; overflow: hidden; margin: var(--space-xl) 0;`
       - `.giantLogoSvg`: `width: 100%; height: auto; display: block;`
       - `.suburbSection`: `border-top: 1px solid rgba(255, 255, 255, 0.12); padding-top: var(--space-l); display: flex; justify-content: space-between; align-items: center; flex-wrap: wrap; gap: var(--space-m); font-size: var(--step--1); color: rgba(255, 255, 255, 0.6);`
       - `.suburbTags`: `display: flex; gap: var(--space-s); flex-wrap: wrap;`
       - `.tag`: `background: rgba(255, 255, 255, 0.08); padding: 4px 10px; border-radius: 4px;`
       - `.projectCard`: `max-width: 360px; background: rgba(255, 255, 255, 0.05); border: 1px solid rgba(255, 255, 255, 0.1); border-radius: var(--variable-radius); padding: var(--space-m); text-decoration: none; color: #ffffff; transition: transform 0.3s var(--ease-out);`
    3. Create `src/components/layout/Footer/Footer.tsx`:
       - Renders `<footer id="main-footer" className={styles.footer} data-theme="dark">`.
       - Renders Haven white geometric emblem.
       - Renders Dot heading "Bespoke Luxury".
       - Renders site links: About, Projects, Expertise, For Architects, Contact.
       - Renders social links: Instagram, Facebook.
       - Renders recently completed project preview for Calibre (2025).
       - Renders giant architectural HAVEN wordmark SVG path vectors (extracted from `legacy_codebase/index.html`).
       - Renders Melbourne suburb tags: Essendon, Aberfeldie, Moonee Ponds, Ascot Vale.
       - Renders Master Builders accreditation badge / legal text and copyright (`©13—26 Haven Constructions`).
    4. Verify with `npx tsc --noEmit`.
  </action>
  <verify>
    <automated>test -f src/components/layout/Footer/Footer.tsx && test -f src/components/layout/Footer/Footer.module.css && npx tsc --noEmit</automated>
    <fails_when>missing Footer files or TypeScript compilation failure</fails_when>
  </verify>
  <done>Comprehensive luxury Footer component built with dark styling, suburb tags, and HAVEN SVG backdrop (NAV-03).</done>
</task>

<task type="code">
  <name>Task 2: Mount Header and Footer in root layout and verify production build</name>
  <files>src/app/layout.tsx</files>
  <action>
    Implement D-09 (global layout shell integration):
    1. Update `src/app/layout.tsx`:
       - Import `<Header>` from `@/components/layout/Header/Header`.
       - Import `<Footer>` from `@/components/layout/Footer/Footer`.
       - Place `<Header />` above `<main>{children}</main>` inside `<SmoothScrollProvider>`.
       - Place `<Footer />` below `<main>{children}</main>` inside `<SmoothScrollProvider>`.
    2. Run `npx tsc --noEmit` and `npm run build` to verify clean compilation across all routes.
  </action>
  <verify>
    <automated>grep -q -- "<Header" src/app/layout.tsx && grep -q -- "<Footer" src/app/layout.tsx && npx tsc --noEmit && npm run build</automated>
    <fails_when>layout missing Header/Footer or Next.js build errors</fails_when>
  </verify>
  <done>Header and Footer are mounted in root layout and full production build succeeds with zero errors.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client DOM layout | Layout mounts Header and Footer across all page routes |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-03 | Information Disclosure | External links in Footer | low | mitigate | Add rel="noopener noreferrer" target="_blank" to Instagram and Facebook links |
</threat_model>

<verification>
1. Footer component: `test -f src/components/layout/Footer/Footer.tsx`
2. Root layout: `grep -q -- "<Header" src/app/layout.tsx && grep -q -- "<Footer" src/app/layout.tsx`
3. TypeScript compiler: `npx tsc --noEmit` exits 0
4. Production build: `npm run build` exits 0
</verification>

<success_criteria>
- [ ] `<Footer>` component built with Melbourne suburb tags and giant HAVEN SVG backdrop (NAV-03).
- [ ] Root layout wraps `<Header />`, `<main>`, and `<Footer />` inside `<SmoothScrollProvider>` (D-09).
- [ ] `npm run build` compiles with zero errors or warnings.
</success_criteria>

<output>
Create `.planning/phases/03-core-navigation-layout-shell/03-02-SUMMARY.md` when done
</output>
