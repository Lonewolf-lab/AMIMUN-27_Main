---
phase: 03-core-navigation-layout-shell
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/layout/Header/Header.tsx
  - src/components/layout/Header/Header.module.css
  - src/components/layout/NavigationOverlay/NavigationOverlay.tsx
  - src/components/layout/NavigationOverlay/NavigationOverlay.module.css
autonomous: true
requirements:
  - NAV-01
  - NAV-02
user_setup: []
estimate:
  tokens: 45000
  raw_tokens: 22000
  tasks: 2
  confidence: high
must_haves:
  truths:
    - "D-01: Header is a fixed pill navigation component that transitions between unscrolled wide mode and compact scrolled mode on scrollY > 50px."
    - "D-02: Header fades out when the footer enters the viewport."
    - "D-03: Burger button features dual-line kinetic animation rotating into an angled cross on toggle."
    - "D-04: Fullscreen NavigationOverlay renders staggered links (Home, Projects, About, Expertise, For Architects, Contact) and contact CTAs."
    - "D-05: NavigationOverlay locks scroll and handles Escape key dismiss."
    - "'npx tsc --noEmit' compiles cleanly."
  artifacts:
    - src/components/layout/Header/Header.tsx
    - src/components/layout/Header/Header.module.css
    - src/components/layout/NavigationOverlay/NavigationOverlay.tsx
    - src/components/layout/NavigationOverlay/NavigationOverlay.module.css
  key_links:
    - "Header.tsx manages menu toggle state and renders NavigationOverlay."
    - "Header.module.css implements responsive max-width transitions and burger animation."
---

<objective>
Build the responsive sticky dual-mode `<Header>` component (NAV-01, D-01, D-02, D-03) and the fullscreen `<NavigationOverlay>` drawer (NAV-02, D-04, D-05) with staggered link reveals, animated hamburger bars, and quick contact CTAs.

Purpose: Delivers the signature floating header bar and architectural modal navigation that enables effortless exploration across the Haven portfolio.
Output: Header and NavigationOverlay components with CSS modules.
</objective>

<execution_context>
@~/.gemini/antigravity/gsd-core/workflows/execute-plan.md
@~/.gemini/antigravity/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-core-navigation-layout-shell/03-CONTEXT.md
@.planning/phases/03-core-navigation-layout-shell/03-RESEARCH.md
@legacy_codebase/styles.css
@legacy_codebase/index.html
</context>

<tasks>

<task type="code">
  <name>Task 1: Build responsive sticky dual-mode Header component</name>
  <files>src/components/layout/Header/Header.tsx, src/components/layout/Header/Header.module.css</files>
  <action>
    Implement NAV-01, D-01, D-02, and D-03:
    1. Create directory `src/components/layout/Header/`.
    2. Create `src/components/layout/Header/Header.module.css`:
       - `.headerWrapper`: fixed position, `inset-inline: 0; top: var(--space-s); z-index: 99; pointer-events: none; transition: opacity 0.4s ease;`
       - `.footerInView`: `opacity: 0; pointer-events: none;`
       - `.navPill`: centered (`margin: 0 auto`), `pointer-events: all`, `display: flex; align-items: center; justify-content: space-between;`, `transition: max-width var(--ease-out) 1.4s, background-color var(--ease-out) 1s, border-color 0.4s;`
       - Unscrolled: `max-width: 420px; background-color: rgba(247, 246, 242, 0.9); backdrop-filter: blur(12px); border: 1px solid var(--color-border); border-radius: 8px; padding: 10px 18px;`
       - Scrolled (`.scrolled`): `max-width: 320px; background-color: rgba(247, 246, 242, 0.98); box-shadow: 0 4px 20px rgba(0, 0, 0, 0.05);`
       - `.logoLink`: renders the Haven 7-polygon mosaic SVG.
       - `.menuButton`: displays "Menu" label and 2 horizontal 1px line spans with CSS transition `transform 0.25s ease`.
       - `.isOpen`: lines translate and rotate to ±12deg; text becomes dark/light aligned.
    3. Create `src/components/layout/Header/Header.tsx`:
       - Mark with `"use client"`.
       - Listen to window scroll (`window.scrollY > 50`) for `isScrolled`.
       - Observe `footer` via `IntersectionObserver` for `isFooterInView`.
       - Manage `isMenuOpen` state and render `<NavigationOverlay isOpen={isMenuOpen} onClose={() => setIsMenuOpen(false)} />`.
    4. Verify with `npx tsc --noEmit`.
  </action>
  <verify>
    <automated>test -f src/components/layout/Header/Header.tsx && test -f src/components/layout/Header/Header.module.css && npx tsc --noEmit</automated>
    <fails_when>missing Header files or TypeScript compilation failure</fails_when>
  </verify>
  <done>Header component built with dual scroll modes, burger animation, and footer collision observer (NAV-01).</done>
</task>

<task type="code">
  <name>Task 2: Build fullscreen NavigationOverlay drawer with staggered link reveals</name>
  <files>src/components/layout/NavigationOverlay/NavigationOverlay.tsx, src/components/layout/NavigationOverlay/NavigationOverlay.module.css</files>
  <action>
    Implement NAV-02, D-04, and D-05:
    1. Create directory `src/components/layout/NavigationOverlay/`.
    2. Create `src/components/layout/NavigationOverlay/NavigationOverlay.module.css`:
       - `.overlay`: `position: fixed; inset: 0; z-index: 98; background-color: #0c388d; color: #ffffff; display: flex; flex-direction: column; justify-content: center; align-items: center; transition: opacity 0.5s var(--ease-out), visibility 0.5s;`
       - `.hidden`: `opacity: 0; visibility: hidden; pointer-events: none;`
       - `.visible`: `opacity: 1; visibility: visible; pointer-events: all;`
       - `.menuList`: `list-style: none; padding: 0; margin: 0; text-align: center;`
       - `.menuItem`: `overflow: clip; margin-bottom: var(--space-s);`
       - `.menuLink`: `font-size: var(--step-4); color: #ffffff; text-decoration: none; display: inline-block; transform: translateY(105%); transition: transform 0.8s cubic-bezier(0.23, 1, 0.32, 1), color 0.2s;`
       - `.visible .menuLink`: `transform: translateY(0%);`
       - `.contactSection`: quick contact buttons for phone (`0483 987 479`) and email (`info@havenconstructions.com.au`) styled with subtle border and hover states.
    3. Create `src/components/layout/NavigationOverlay/NavigationOverlay.tsx`:
       - Mark with `"use client"`.
       - Props: `isOpen: boolean; onClose: () => void;`
       - Handle Escape key press to trigger `onClose()`.
       - Body scroll lock effect on `isOpen` toggle.
       - Route change listener or link clicks trigger `onClose()`.
       - Navigation links: `Home (/)`, `Projects (/projects)`, `About (/about)`, `Expertise (/expertise)`, `For Architects (/for-architects)`, `Contact (/contact)`.
    4. Verify with `npx tsc --noEmit`.
  </action>
  <verify>
    <automated>test -f src/components/layout/NavigationOverlay/NavigationOverlay.tsx && test -f src/components/layout/NavigationOverlay/NavigationOverlay.module.css && npx tsc --noEmit</automated>
    <fails_when>missing NavigationOverlay files or TypeScript compilation failure</fails_when>
  </verify>
  <done>NavigationOverlay drawer built with staggered link animations, contact CTAs, and keyboard dismiss (NAV-02).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client window DOM | Header and Overlay listen to browser scroll, keyboard, and resize events |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-01 | Denial of Service | Header scroll listener | low | mitigate | Use passive scroll event listeners and IntersectionObserver instead of continuous layout thrashing |
| T-03-02 | Usability / Accessibility | NavigationOverlay | low | mitigate | Implement Escape key dismissal and ensure touch targets exceed 44px |
</threat_model>

<verification>
1. Header component: `test -f src/components/layout/Header/Header.tsx`
2. Navigation overlay: `test -f src/components/layout/NavigationOverlay/NavigationOverlay.tsx`
3. TypeScript validation: `npx tsc --noEmit`
</verification>

<success_criteria>
- [ ] Responsive sticky dual-mode `<Header>` component built with smooth scroll transitions (NAV-01).
- [ ] Fullscreen `<NavigationOverlay>` drawer built with staggered link reveals and contact CTAs (NAV-02).
- [ ] TypeScript compiles cleanly with zero errors.
</success_criteria>

<output>
Create `.planning/phases/03-core-navigation-layout-shell/03-01-SUMMARY.md` when done
</output>
