---
phase: 06-seo-performance-production-polish
plan: 02
type: execute
wave: 2
depends_on:
  - "06-01"
files_modified:
  - next.config.ts
autonomous: true
requirements:
  - PERF-02
  - PERF-03
user_setup: []
estimate:
  tokens: 45000
  raw_tokens: 22000
  tasks: 2
  confidence: high
must_haves:
  truths:
    - "D-05: next.config.ts enables AVIF and WebP modern image formats with caching headers."
    - "D-06: All image containers have explicit aspect ratios ensuring 0 Cumulative Layout Shift (CLS)."
    - "D-07: Next.js production build succeeds with clean static page generation across all routes."
    - "D-08: Complete visual parity and responsive scaling verified across viewports."
    - "'npm run build' and 'npx tsc --noEmit' pass cleanly."
  artifacts:
    - next.config.ts
  key_links:
    - "next.config.ts optimizes image delivery and security headers."
---

<objective>
Harden application performance, eliminate external asset dependencies (PERF-02), optimize Next.js image pipeline with modern formats (AVIF/WebP), and verify sub-second LCP and zero layout shift across all viewports (PERF-03).

Purpose: Delivers a lightning-fast, production-ready luxury web application ready for deployment.
Output: Optimized next.config.ts, verified build artifacts, and comprehensive production audit.
</objective>

<execution_context>
@~/.gemini/antigravity/gsd-core/workflows/execute-plan.md
@~/.gemini/antigravity/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-seo-performance-production-polish/06-CONTEXT.md
@.planning/phases/06-seo-performance-production-polish/06-RESEARCH.md
@next.config.ts
</context>

<tasks>

<task type="code">
  <name>Task 1: Optimize Next.js configuration and image pipeline</name>
  <files>next.config.ts</files>
  <action>
    Implement PERF-02 (Next.js Image Pipeline & Header Hardening):
    1. Update `next.config.ts`:
       - Enable modern image formats: `formats: ['image/avif', 'image/webp']`.
       - Configure device sizes: `[640, 750, 828, 1080, 1200, 1920, 2048, 3840]`.
       - Add security and caching headers (`X-DNS-Prefetch-Control`, `X-Frame-Options`, `X-Content-Type-Options`, `Referrer-Policy`).
       - Maintain remote pattern whitelisting for `haven.api.jtbadev.com` and `www.havenconstructions.com.au`.
    2. Verify with `npx tsc --noEmit`.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
    <fails_when>TypeScript syntax error in next.config.ts</fails_when>
  </verify>
  <done>next.config.ts configured for AVIF/WebP image optimization and security headers (PERF-02).</done>
</task>

<task type="code">
  <name>Task 2: Validate production build, zero CLS, and responsive layout</name>
  <files>next.config.ts</files>
  <action>
    Implement PERF-03 (Performance & Zero Layout Shift Verification):
    1. Execute full production build: `npm run build`.
    2. Verify all 13 routes and metadata routes (`/sitemap.xml`, `/robots.txt`) compile cleanly.
    3. Verify all image wrappers have fixed CSS aspect ratios (`16/10`, `4/5`, `1/1`) preventing layout shift.
    4. Confirm zero linter or TypeScript warnings.
  </action>
  <verify>
    <automated>npm run build</automated>
    <fails_when>Next.js build failure or prerendering error</fails_when>
  </verify>
  <done>Production build passes with all routes prerendered and zero layout shift (PERF-03).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Image Optimizer / CDN | Next.js Image optimization pipeline processing remote image buffers |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-02 | Denial of Service | next/image | low | mitigate | Restrict remote patterns to whitelisted Haven domains only with defined deviceSizes |
</threat_model>

<verification>
1. Configuration check: `grep -q "image/avif" next.config.ts`
2. Full suite build: `npm run build` exits 0 with all routes prerendered
3. Static assets check: `test -f public/fonts/SuisseIntl-Regular.woff2`
</verification>

<success_criteria>
- [ ] Modern image formats (AVIF, WebP) enabled in Next.js pipeline (PERF-02).
- [ ] Remote patterns restricted to whitelisted Haven domains (PERF-02).
- [ ] All image wrappers provide explicit aspect ratios ensuring 0 CLS (PERF-03).
- [ ] `npm run build` succeeds cleanly with all static routes and metadata handlers pre-rendered.
</success_criteria>

<output>
Create `.planning/phases/06-seo-performance-production-polish/06-02-SUMMARY.md` when done
</output>
