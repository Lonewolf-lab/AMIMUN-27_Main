---
phase: 06-seo-performance-production-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/seo/StructuredData.tsx
  - src/app/layout.tsx
  - src/app/sitemap.ts
  - src/app/robots.ts
  - src/app/icon.svg
autonomous: true
requirements:
  - PERF-01
user_setup: []
estimate:
  tokens: 45000
  raw_tokens: 22000
  tasks: 2
  confidence: high
must_haves:
  truths:
    - "D-01: Root layout injects comprehensive JSON-LD HomeAndConstructionBusiness structured data."
    - "D-02: Next.js Metadata API configures OpenGraph, Twitter, and luxury builder keywords across all pages."
    - "D-03: Dynamic /sitemap.xml and /robots.txt are generated via App Router route handlers."
    - "D-04: High-fidelity SVG brand icon rendered as favicon."
    - "'npx tsc --noEmit' and 'npm run build' compile cleanly."
  artifacts:
    - src/components/seo/StructuredData.tsx
    - src/app/sitemap.ts
    - src/app/robots.ts
    - src/app/icon.svg
  key_links:
    - "src/app/layout.tsx renders StructuredData component."
    - "src/app/sitemap.ts imports getAllProjects from src/lib/content."
---

<objective>
Configure comprehensive SEO, OpenGraph tags, JSON-LD LocalBusiness schema (PERF-01), dynamic sitemaps, robots.txt, and vector brand icons for search engine discovery and premium social link sharing.

Purpose: Positions Haven Constructions as the authoritative luxury home builder across Essendon, Aberfeldie, Moonee Ponds, and Ascot Vale in search indexes.
Output: StructuredData component, sitemap.ts, robots.ts, icon.svg, and enhanced layout metadata.
</objective>

<execution_context>
@~/.gemini/antigravity/gsd-core/workflows/execute-plan.md
@~/.gemini/antigravity/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-seo-performance-production-polish/06-CONTEXT.md
@.planning/phases/06-seo-performance-production-polish/06-RESEARCH.md
@src/app/layout.tsx
@src/lib/content.ts
</context>

<tasks>

<task type="code">
  <name>Task 1: Build StructuredData component and enhance RootLayout metadata</name>
  <files>src/components/seo/StructuredData.tsx, src/app/layout.tsx</files>
  <action>
    Implement PERF-01 (Structured Data & Global Metadata):
    1. Create directory `src/components/seo/`.
    2. Create `src/components/seo/StructuredData.tsx`:
       - Emits `<script type="application/ld+json">` with `HomeAndConstructionBusiness` schema.
       - Details: Name ("Haven Constructions"), telephone, email, West Footscray studio address, geo coordinates, areaServed (Essendon, Aberfeldie, Moonee Ponds, Ascot Vale, Melbourne), priceRange ("$$$$").
    3. Update `src/app/layout.tsx`:
       - Configure `metadataBase: new URL("https://www.havenconstructions.com.au")`.
       - Title template: `%s | Haven Constructions`.
       - Comprehensive keywords for luxury residential building and northwest Melbourne suburbs.
       - Full OpenGraph and Twitter card metadata.
       - Mount `<StructuredData />` in `<head>` or body.
    4. Verify with `npx tsc --noEmit`.
  </action>
  <verify>
    <automated>test -f src/components/seo/StructuredData.tsx && npx tsc --noEmit</automated>
    <fails_when>missing StructuredData or TypeScript errors in layout</fails_when>
  </verify>
  <done>StructuredData and global metadata configured (PERF-01).</done>
</task>

<task type="code">
  <name>Task 2: Build dynamic sitemap.ts, robots.ts, and vector icon.svg</name>
  <files>src/app/sitemap.ts, src/app/robots.ts, src/app/icon.svg</files>
  <action>
    Implement PERF-01 (Dynamic Metadata Routes & Brand Icon):
    1. Create `src/app/sitemap.ts`:
       - Imports `getAllProjects()` from `@/lib/content`.
       - Returns sitemap entries for `/`, `/about`, `/expertise`, `/for-architects`, `/projects`, `/contact`, plus all `/projects/[slug]` entries with `lastModified` and `priority`.
    2. Create `src/app/robots.ts`:
       - Configures allow rules for all crawlers and points to `https://www.havenconstructions.com.au/sitemap.xml`.
    3. Create `src/app/icon.svg`:
       - High-fidelity SVG vector of Haven geometric mark in signature blue `#0c388d`.
    4. Verify with `npx tsc --noEmit` and `npm run build`.
  </action>
  <verify>
    <automated>test -f src/app/sitemap.ts && test -f src/app/robots.ts && test -f src/app/icon.svg && npx tsc --noEmit && npm run build</automated>
    <fails_when>missing metadata route files or Next.js build errors</fails_when>
  </verify>
  <done>Dynamic sitemap, robots.txt, and SVG brand icon verified in production build (PERF-01).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Search Engine Indexing / Sitemap | URLs exposed to public web crawlers |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-01 | Information Disclosure | sitemap.ts | low | mitigate | Only include public, intended marketing and case study routes |
</threat_model>

<verification>
1. StructuredData component: `test -f src/components/seo/StructuredData.tsx`
2. Sitemap route: `test -f src/app/sitemap.ts`
3. Robots route: `test -f src/app/robots.ts`
4. SVG Icon: `test -f src/app/icon.svg`
5. Build check: `npm run build` exits 0 with sitemap.xml and robots.txt prerendered
</verification>

<success_criteria>
- [ ] JSON-LD `HomeAndConstructionBusiness` structured data rendered.
- [ ] OpenGraph and Twitter cards configured across all routes.
- [ ] `sitemap.xml` and `robots.txt` dynamically generated in App Router.
- [ ] SVG favicon served via `src/app/icon.svg`.
- [ ] `npm run build` passes with zero errors.
</success_criteria>

<output>
Create `.planning/phases/06-seo-performance-production-polish/06-01-SUMMARY.md` when done
</output>
